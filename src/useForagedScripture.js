import * as React from 'react'




export const useForagedScripture = (pasage) => {
    
    const audioPlayer = React.useMemo(() => {return new Audio()}, [passageBuffer])

    
    

    // Start the async check
    checkForage();

    // Return the hook
    return passageBlob;
}